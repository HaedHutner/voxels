add_subdirectory("glm")
link_libraries(glm)

add_subdirectory("glfw")
link_libraries(glfw)

add_subdirectory("glad")
link_libraries(glad)

add_subdirectory("fastnoise")
link_libraries(fastnoise)

# add_subdirectory("stb")
# link_libraries(stb)